subprojects {

    buildscript {
        repositories {
            jcenter()
        }
        dependencies {
            classpath "com.google.protobuf:protobuf-gradle-plugin:0.8.8"
        }
    }

    repositories {
        jcenter()
    }
}
